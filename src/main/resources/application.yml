server:
  port: 8090

spring:
  profiles:
    active: @profiles.active@   # @profiles.active@
  mvc:
    throw-exception-if-no-handler-found: true
  application:
    name: landa-mas-docker-demo    # 项目名称尽量用小写
